// selecting elements by get element by id 

console.log(document.getElementById("main-heading")); // we can select elements through id by this method and in the document it returns an object 
console.log(typeof document.getElementById("main-heading")); // here by the type we can see that it is an object 

const mainHeading = document.getElementById("main-heading"); // we can also store in the variables 
console.log(mainHeading);

// the get element by id can only target elements with an id  
